services:
  senfoniscan:
    build: .
    image: senfoniscan:latest
    volumes:
      - ./reports:/app/reports
    env_file:
      - .env
    # Example usage: docker-compose run senfoniscan -u example.com
